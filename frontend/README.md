# SafeBite Government Portal Frontend

Create a Vite React app in the project root:

```powershell
npm create vite@latest frontend -- --template react
cd frontend
npm install
npm install react-router-dom
```

Then replace the generated `src` directory with this `src` directory and run:

```powershell
npm run dev
```
